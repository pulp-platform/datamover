// Coverage hierarchy config for the datamover standalone testbench
// Author: Lionnus Kesting <lkesting@iis.ee.ethz.ch>

// The last directive that matches wins, so the excludes must follow the include.

// Exclude the TB and the TB core, include the DUT
-tree tb_datamover
+tree tb_datamover.i_dut
