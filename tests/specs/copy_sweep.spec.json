{
  "name": "copy_sweep",
  "op": "copy",
  "hw_config": "default",
  "description": "M and N: max 65535 (16-bit matrix_dim regs). C*M*N: max ~1.3 MiB (the 4 MB TB data memory holds 3 buffers per test).",
  "max_tests": 30,
  "seed": 1,
  "axes": {
    "SIZE_C": [1, 3],
    "SIZE_M": {"min": 1, "max": 2048},
    "SIZE_N": {"min": 1, "max": 2048}
  }
}
